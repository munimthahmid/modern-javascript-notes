let age = 11;

let type = age > 18 ? "adult" : "child";

let type2 = age > 18 ? "adult" : age > 10 ? "young" : "child";

console.log(type2);

let a = 5;

let check = a > 5 ? true : false; //no need

let check2 = a > 5; //use like this when our values are simply true,false
